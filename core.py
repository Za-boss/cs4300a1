from domains.wgc import *
def BFS(domain):
    path = []
    initialState = domain
    frontier = [initialState]
    seen = set()
    seen.add(initialState)
    sol_cost = 0
    nodesGenerated = 0
    nodesExpanded = 0
    frontierSize = 0
    while frontier:
        return None

def IDS(domain):
    frontier = []
    depth = 1
    sol_cost = 0
    nodesGenerated = 0
    nodesExpanded = 0
    frontierSize = 0
    while frontier != None:
        return None